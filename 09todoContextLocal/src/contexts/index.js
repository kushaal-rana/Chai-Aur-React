//index.js is used to import and export all the contexts from the context folder

import { TodoContext, TodoProvider, useTodo } from "./TodoContext.js";

export { TodoContext, TodoProvider, useTodo };
